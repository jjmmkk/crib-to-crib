// @todo
// The distance between two points in a grid is
// sqrt( (B->x - A->x)^2 + (B->y - A->y)^2 )
// where `A` and `B` are the two points

define( [ 'gmaps' ], function( gmaps ) {
    var pointsDistance = function( a, b ) {
        //
    }

    return {
        tsp: function( points ) {
            //
        }
    };
} );
